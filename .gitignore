# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
dist-ssr/
.vite/

# Environment / secrets
.env
.env.*
!.env.example

# Local data (SQLite leftovers, uploads, scratch)
backend/data/
landing/uploads/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Lockfiles from other package managers (this repo uses pnpm)
package-lock.json
yarn.lock

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.local

# Claude Code — machine-local settings and tooling caches
.claude/settings.local.json
.code-review-graph/

# Large media and archives (host these on a CDN, not in git)
*.zip
*.mov
*.avi
